#include <program1.h>

funcion entero mayor2v(entero uno, entero dos) {
entero resul;
si(uno > dos) entonces
    resul = uno;
    sino
    resul = dos;
    finSi;
retorna(resul);
}

funcion real mayor2v(real uno, real dos) {
real resul;
si(uno > dos) entonces
    resul = uno;
    sino
    resul = dos;
    finSi;
retorna(resul);
}